# --- Needed to run the stub pipeline (scripts/*.py) — install just these two ---
pydantic>=2.5
pyyaml>=6.0

# --- Needed only for real stage implementations, not for the stubs ---
numpy                  # stage2/3/4 math
opencv-python          # stage2 perception (Lauren)
mujoco                 # stage4 simulation (Azeem)
lerobot                # stage3 policy training/inference (Bidipta + Azeem)
openvino               # stage5 Intel optimization (Lauren)
speechmatics-python    # stage1 ASR (Alex)
anthropic              # stage1 instruction parsing (Alex)

# --- Dev / test ---
pytest
